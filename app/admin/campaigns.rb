ActiveAdmin.register Campaign do

  # See permitted parameters documentation:
  # https://github.com/activeadmin/activeadmin/blob/master/docs/2-resource-customization.md#setting-up-strong-parameters
  #
  # Uncomment all parameters which should be permitted for assignment
  #
  #permit_params :shop_id, :name, :tag, :segmentation_size, :delivered, :title, :body, :url, :impressions, :clicks, :release_date, :automatic_campaign_id
  #
  # or
  #
  # permit_params do
  #   permitted = [:shop_id, :name, :tag, :segmentation_size, :delivered, :title, :body, :url, :impressions, :clicks, :release_date, :automatic_campaign_id]
  #   permitted << :other if params[:action] == 'create' && current_user.admin?
  #   permitted
  # end
  
end
