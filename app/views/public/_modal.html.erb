<style type="text/css">
.aplicatify-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  width: 30%;
  min-width: 300px;
  max-width: 399px;
  max-height: 247px;
  transform: translate(-50%, -50%);
  text-align: left;
  background: rgba(255, 255, 255, 1);
  transition: opacity .25s;
  box-shadow: 0 3px 13px 0 rgba(0,0,0,0.19);
  border-radius: 5px;
  padding: 1em 2em;
  z-index: 2147483647;
  border-radius: 4px;
  box-shadow: 3px 3px 6px rgba(0, 0, 0, 0.4);
}

.app-modal-title {
    font-size: 18px;
    padding-bottom: 5px;
    font-weight: bold;
  }

  .app-modal-body {
    position: relative;
    font-size: 16px;
    padding-bottom: 25px;
  }

.aling-bottom {
    position: relative;
    width: 100%;
    float: right;
}

.btn {
  display: inline-block;
  font-weight: 400;
  text-align: center;
  white-space: nowrap;
  vertical-align: middle;
  padding: .375rem .75rem;
  font-size: 1rem;
  line-height: 1.5;
  border-radius: .25rem;
  transition: color .15s ease-in-out, background-color .15s ease-in-out, border-color .15s ease-in-out, box-shadow .15s ease-in-out;
  min-width: 114px;
  min-height: 39px;
  max-width: 152px;
  width: 38%;
  max-height: 39px;
  height: 16%;
  border-radius: 4px;
  box-shadow: 0px 3px 6px rgba(0, 0, 0, 0.16);
}

.close_button {
  color: #007bff !important;
  background-color: #FFFFFF !important;
  border-color: #FFFFFF !important;
}

.accept {
  color: #fff !important;
  background-color: #007bff !important;
  border-color: #007bff !important;
}
</style>

<div id="modal_<%=optin.kind%>" class="aplicatify-modal" style="background: #<%= optin.background_color %>; display: none;">
  <div id="modal_optin_title" class="app-modal-title" style="color: #<%= optin.text_color %>">
    <%=optin.title%>
  </div>

  <div id="modal_optin_body" class="app-modal-body" style="color: #<%= optin.text_color %>">
    <%=optin.body%>
  </div>

  <div class="aling-bottom">
    <button id="modal_optin_decline_button" class="btn btn-no close_button" onClick="closePopup('<%=optin.kind%>')"><%=optin.decline_button%></button>
    <button id="modal_optin_accept_button" class="btn btn-yes accept" style="float: right;" onClick="showPrompt('<%=optin.kind%>')"><%=optin.accept_button%></button>
  </div>
</div>